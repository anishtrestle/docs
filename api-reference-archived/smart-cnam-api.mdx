---
title: "Smart CNAM API (Archived)"
description: "Validates a phone number and provides person or business name - Archived Version 3.1"
api: "GET /3.1/cnam"
---

<Warning>
  This is an archived version of the API. Please use the [Current Smart CNAM
  API](/api-reference/smart-cnam-api) for new integrations.
</Warning>

## Overview

Smart CNAM API validates a phone number and provides person or business name associated to the phone. This is a lightweight API perfect for caller ID applications.

**Version:** 3.1 (Archived)

## Endpoint

<RequestExample>

```bash
GET https://api.trestleiq.com/3.1/cnam?phone={phone}
```

</RequestExample>

## Parameters

<ParamField query="phone" type="string" required>
  The phone number in E.164 or local format. **Example:** `2069735100`
</ParamField>

<ParamField query="phone.country_hint" type="string">
  The ISO-3166 alpha-2 country code of the phone number. **Example:** `US`
</ParamField>

## Headers

<ParamField header="x-api-key" type="string" required>
  Your API key for authentication.
</ParamField>

## Code Examples

<CodeGroup>

```bash cURL
curl --request GET \
  --url "https://api.trestleiq.com/3.1/cnam?phone=2069735100&phone.country_hint=US" \
  --header "x-api-key: YOUR_API_KEY" \
  --header "Accept: application/json"
```

```javascript JavaScript
const response = await fetch(
  "https://api.trestleiq.com/3.1/cnam?phone=2069735100&phone.country_hint=US",
  {
    headers: {
      "x-api-key": "YOUR_API_KEY",
      Accept: "application/json",
    },
  }
);
const data = await response.json();
```

</CodeGroup>

## Response

<ResponseField name="id" type="string">
  The persistent ID of the phone number.
</ResponseField>

<ResponseField name="is_valid" type="boolean">
  True if the phone number is valid.
</ResponseField>

<ResponseField name="belongs_to" type="object">
  The primary owner of the phone number with name information.
</ResponseField>

<ResponseField name="error" type="object">
  Error information if the request failed.
</ResponseField>

<ResponseField name="warnings" type="array">
  Warnings returned as part of the response, if applicable. Possible values
  include: `Invalid Input`, `International number. Not authorized`, or `Missing
  Input`.
</ResponseField>
